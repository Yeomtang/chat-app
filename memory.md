# 프로젝트 메모리

## 프로젝트 개요
방송 제작 배경 LED 화면용 실시간 익명 채팅 시스템

## 핵심 요구사항
- **용도**: 방송 배경 LED 화면에 상시 노출되는 채팅창
- **참여자**: 현장 토론자 + 관객 (익명 채팅)
- **입장 방식**: QR코드 스캔 → 웹페이지 입장 (앱 다운로드 없음)
- **네트워크**: LTE 환경에서도 원활하게 동작해야 함 (Wi-Fi 의존 X)
- **채팅 UI**: iMessage 스타일, 메시지가 위로 계속 올라오는 구조
- **익명성**: 관객은 익명으로 참여

## 화면 구성
- 비율: 2:3 세로형 (핸드폰 세로 비율 기준, LED 특성 반영)
- 화면 수: 1개
- 테마: 라이트모드
- 채팅 스타일: 깔끔한 말풍선, 좌측 정렬 (첨부 이미지 참고)
- 입장 시 닉네임: **직장인 피로 테마 랜덤 닉네임 자동 추천**
  - 예: "야근 3년차", "퇴근하고 싶다", "월요일이 싫은 사람" 등
  - 닉네임은 LED 화면 말풍선에도 표시됨

## 화면 모드 (2가지)
1. **채팅 모드 (기본)**: 채팅창이 전체 화면 차지, iMessage 스타일로 위로 흐름
2. **질문 모드 (제작진 트리거)**: 채팅창 블러 처리 → 공통 질문 팝업 등장
   - 예시 질문: "지금 본인의 회사 생활에 만족하십니까?"
   - 답변 형식 2가지:
     - **객관식 (Yes/No)**: 실시간 비율 시각화
       - YES 원 / NO 원 두 개의 원형 클러스터
       - 투표마다 작은 원(공)이 해당 원으로 날아들어오는 애니메이션
       - 공들이 반대쪽 원으로 이동하는 흐름으로 실시간 비율 변화 표현
       - 답변 변경 가능, 단 **30초 타이머** 제한
       - 30초 카운트다운: **LED 화면 + 관객 핸드폰 양쪽에 표시**
       - 타이머 종료 후 최종 결과 고정
       - 흐름: 질문 등장 → 30초 카운트다운 → 실시간 공 이동 → 종료 → 결과 확정
     - **주관식 (자유 텍스트)**: 개별 답변 흘려보내기 or 워드클라우드

## 관리자 인터페이스 (제작진용)
- 질문을 **미리 여러 개 작성**해두고 순서대로 실행
- 기능: 질문 입력/편집 → 순서 정렬 → 시작 버튼 → 결과 확인
- 방송 중 즉흥적으로 질문 추가도 가능해야 할 듯

## 전체 화면 흐름
1. **채팅 모드**: iMessage 스타일 익명 채팅, 위로 스크롤
2. **질문 모드 진입** (제작진 트리거): 채팅 블러 → 질문 텍스트 + 30초 타이머 등장
3. **투표 중**: YES/NO 공 애니메이션 실시간, 타이머 LED+핸드폰 동시 표시
4. **타이머 종료**: 질문 텍스트·타이머 사라짐 → **결과(원형 클러스터)만 화면에 floating**
5. **결과 유지**: MC/토론자 코멘트 시간, 제작진이 수동으로 다음 전환
6. **채팅 모드 복귀** (제작진 수동 트리거): 다시 익명 채팅, 관객이 토론자에게 질문/의견 전송

## 미결 사항 / 논의 중
- ✅ 질문/투표 모드 중 채팅 입력 완전 차단 → 오직 답변에만 집중
- ✅ 투표 후 핸드폰 화면: 내가 선택한 쪽 하이라이트 + 실시간 비율 표시
- 채팅 모드 ↔ 질문 모드 전환 시 LED 화면 애니메이션
- 주관식 결과 표시 방식 세부 (흘려보내기 vs 워드클라우드 선택)

## 진행 상황 (구현 단계)
- ✅ **1단계 완료 + 배포**: 채팅 시스템 (QR 입장 가정, 닉네임 자동배정 625개 풀, iMessage 스타일, LED 표시)
  - 관객: https://chat-app-6kl5.onrender.com/
  - LED: https://chat-app-6kl5.onrender.com/led
  - ⚠️ Render 무료 플랜은 비활성 시 서버 슬립 → 첫 접속 시 30~60초 지연 가능. 실사용(12월) 전 유료 플랜 전환 검토 필요
- 🔄 **2단계 구현 중**: Yes/No 투표 + 공 애니메이션 + 30초 타이머
  - 서버(server.js): `mode: 'chat'|'voting'|'result'` 상태머신 추가. `votes` 객체는 clientId → 'yes'|'no'로 저장(중복 방지). `identify` 이벤트로 클라이언트 영구 ID 수신. `admin:startVoting/endVoting/returnToChat/getState` 이벤트로 제작진 제어. 타이머는 서버가 `votingEndTime`(epoch ms)을 계산해 브로드캐스트, 각 클라이언트는 로컬에서 카운트다운만 표시(서버 권위 방식).
  - LED 화면(led.html): 채팅 레이어 블러 처리 + 투표 오버레이. Canvas 기반 공 파티클 애니메이션(클라이언트별 1개 공, YES/NO 클러스터로 sunflower 패킹 배치, 부드러운 이동 애니메이션). voting 모드에서 질문+타이머 표시, result 모드에서는 질문/타이머 숨기고 결과 비율만 floating 표시.
  - 관객 화면(chat.html): localStorage 기반 영구 `clientId` 생성 → 소켓 연결 시 `identify`로 서버에 전송(재접속해도 동일 투표자로 인식). 투표 오버레이(질문+타이머+YES/NO 버튼+실시간 비율 바) 추가, 투표 중엔 채팅 입력 완전 차단(블러 처리). 투표 후 내가 선택한 버튼 하이라이트 유지. 등장 모션 추가: 채팅 블러(0.5s)와 동시에 투표 박스(`#voting-card`)가 중앙에서 작게 시작해 살짝 튕기듯 커지며 등장(`scale 0.4→1.05→1`, 0.5s). voting→result 전환처럼 오버레이가 계속 떠있는 동안은 재생되지 않고, chat 모드 복귀 후 다음 질문에서만 다시 재생됨.
  - LED 화면(led.html): YES/NO 공·라벨 크기 1.5배 확대(`BALL_SCALE` 상수로 일괄 적용, 클러스터 반경도 비례 확대).
  - 임시 관리자 화면(admin.html): 정식 4단계 화면 전까지 사용할 임시 컨트롤 패널. 현재 상태 모니터링(모드/질문/YES·NO 카운트/남은 시간), 질문 사전 등록(localStorage 저장, 순서대로 실행), 즉석 질문 시작, 투표 강제 종료, 채팅 모드 복귀 기능 포함.
  - (Gemini 작업) 채팅 히스토리 중복/미로딩 버그 수정 — `history`/`chat` 리스너를 `enterChat()` 시점이 아니라 소켓 연결 시점에 등록하도록 변경.
  - (Gemini 작업) 닉네임 배정을 서버 왕복 없이 클라이언트가 즉시 로컬에서 뽑도록 최적화(UX 응답속도 개선) — 단, 이 변경으로 동시접속자 간 닉네임 중복 가능성이 생김(이전엔 서버 공유 풀이 순서대로 배정해 중복 방지).
  - 위 중복 문제 수정: server.js에 `activeNicknames`(socket.id→닉네임) 추적 + `claimNickname`/`nicknameReassigned` 이벤트 추가. 클라이언트는 화면에 즉시 로컬 닉네임을 표시하되, 연결 즉시 서버에 `claimNickname`으로 등록 요청 → 동시 중복이면 서버가 안 쓰이는 닉네임으로 교체해 `nicknameReassigned`로 내려줌. 즉시 표시 UX는 유지하면서 중복만 사후 교정. socket.io-client 통합 테스트로 충돌 감지/교체, 비충돌 시 미반응, 기존 투표 상태머신(시작→카운트→타이머만료→result) 모두 정상 동작 확인.
  - 버그 수정: 아무도 투표 안 한 상태(total=0)에서 결과 화면에 "NO 100%"로 잘못 표시되던 문제. `noPct = 100 - yesPct` 계산이 total=0일 때 yesPct=0 → noPct=100이 되는 결함. chat.html(`updateRatio`)·led.html(result 분기) 둘 다 `noPct = c.total ? 100 - yesPct : 0`으로 수정.
  - LED 화면(led.html) 투표 중 실시간성 검토: 처음엔 투표 중(voting)에도 숫자 비율 배너를 추가했었으나, 사용자 피드백으로 되돌림 — 숫자 배너 대신 이미 구현되어 있던 공 클러스터 애니메이션(투표할 때마다 공이 해당 클러스터로 이동, 답변 변경 시 반대쪽으로 날아가는 흐름)이 카운트다운 동안의 실시간 현황을 보여주는 역할을 함. 결과 배너(%)는 타이머 종료 후 result 모드에서만 표시(원래 동작으로 복귀).
  - LED 화면(led.html) 공 합체(merge) 효과 추가: 재미 요소로, 작은 공(1표) 10개가 모이면 중간 크기 공(10표 단위)으로, 중간 공 10개가 모이면 큰 공(100표 단위)으로 합쳐지는 3단계 표현. 기존엔 클라이언트(투표자)별로 공 1개씩 추적(`particles` Map)했으나, 서버가 이미 내려주는 집계값(`state.counts`/`voteUpdate`의 `data.counts`)을 기반으로 한 단계별 슬롯 배열(`tierState[side].{ones,tens,hundreds}`) 방식으로 전환 — 표 개수(`total`)를 10진법처럼 분해(`hundreds=floor(n/100)`, `tens=floor(n%100/10)`, `ones=n%10`)해 각 단계 슬롯 개수를 맞춤. 단계별 반지름은 `BALL_SCALE` 기준 1.8배씩 커짐(`TIER_SCALE`). 배치는 동심원 방식 패킹(`packTier`): 큰 단계(hundreds)를 중심에 먼저 배치하고 그 바깥 반지름(`outerExtent`)만큼 띄워서 중간(tens)·바깥(ones) 단계를 순서대로 sunflower 패킹. 새로 생긴 tens/hundreds 공은 등장 시 `easeOutBack` 이징으로 짧게(450ms) 팝 효과를 주어 "합체" 순간을 강조. 개별 투표자 단위 공 식별(특정 유저가 어느 공인지)은 사라지고 집계 수치 기반 표현으로 단순화됨 — 디자인 트레이드오프로 인지하고 진행. Node로 단계 계산 로직(0/1/9/10/99/100/101/999/1234표 등 경계값)과 패킹 함수(좌표 NaN 없음, count 0~50) 검증 완료.
  - LED 화면(led.html) 채팅 일시정지 기능 추가: 다수가 동시에 채팅을 보낼 때 메시지가 너무 빨리 올라가 진행자/패널이 읽을 시간이 부족한 문제 해결.
    - 1차 시도(폐기): LED 화면(채팅 영역) 자체를 클릭/더블클릭해서 제어하는 방식 — 구현 후 사용자가 "LED 화면에서 마우스 클릭이 안 될 것 같다"고 판단해 폐기.
    - 최종 방식: 제어 주체를 관리자 화면(admin.html)으로 이전. server.js의 `appState.chatPaused`(boolean)를 신설해 `publicState()`에 포함시키고, `admin:pauseChat`/`admin:resumeChat` 소켓 이벤트를 추가해 누르면 전체 클라이언트에 `chatPauseChange` 이벤트를 broadcast. led.html은 이 값을 그대로 따라 화면 렌더링만 멈춤/재생(`pauseChat()`/`resumeChat()` 로직은 기존 유지: 정지 중엔 들어오는 메시지를 버퍼(`messages`, 최대 300개)에는 계속 쌓되 DOM에는 미반영, 재생 시 버퍼의 최신 메시지로 즉시 점프 — 밀린 메시지를 점진적으로 따라잡지 않음). LED 화면 자체엔 정지 표시를 넣지 않되, 관리자 화면에는 현재 상태(실시간 표시 중/정지됨)와 토글 버튼을 표시해 제작진이 헷갈리지 않게 함.
    - 핵심 요건: 관리자가 LED 채팅창을 멈춰도 **관객의 채팅 송수신은 영향 없음** — server.js의 `chat` 이벤트 핸들러는 `mode === 'chat'`일 때만 동작하고 `chatPaused`와는 무관하게 항상 정상 브로드캐스트함(LED 쪽의 표시 여부만 클라이언트에서 따로 제어). socket.io-client 통합 테스트로 (1) 초기 chatPaused=false, (2) pauseChat 호출 시 전체에 chatPaused:true broadcast, (3) 정지 중에도 관객 채팅 송수신 정상, (4) state 재조회시 정지 상태 유지, (5) resumeChat 시 false broadcast — 모두 확인.
  - 결과(result) 화면에 질문 유지: 기존엔 타이머 종료 후 질문·타이머가 모두 사라지고 공 클러스터+%만 표시됐으나, 무엇에 대한 결과인지 알 수 없다는 피드백으로 변경 — 타이머만 숨기고 질문 텍스트는 상단에 계속 표시(led.html result 분기 `votingQuestion.style.display='block'`, chat.html도 동일하게 질문 유지).
  - LED 채팅 표시 개수 확대: `MAX_VISIBLE` 8→40. 8개일 땐 화면 하단만 채워지고 상단(LIVE CHAT 헤더 아래)이 비어 보이는 문제 — 컨테이너가 `justify-content: flex-end`+`overflow: hidden` 구조라 개수만 늘리면 화면이 가득 차고, 넘치는 위쪽은 상단 페이드 그라디언트 아래로 자연스럽게 잘림.
  - 채팅 글자 수 100자 제한 추가: 관객 폰(chat.html) textarea `maxlength=100` + 붙여넣기 우회 대비 JS 이중 방어 + 80자부터 카운터 표시(100자 도달 시 빨간색). 서버(server.js)도 `MAX_CHAT_LENGTH=100`으로 초과분 자르고 빈 메시지 차단(클라이언트 제한 우회 대비). 근거: LED(FHD 전체화면, 폭 720px)에서 한 줄 약 30자 → 5줄이면 150자 이상이라 화면 점유 과다, 방송 가독성 기준으로 2~3줄(100자)로 결정.
  - 리액션(하트/붐업) 기능 추가: 관객 폰(chat.html) 입력창 왼쪽에 ❤️/👍 원형 버튼 2개 — 누르면 버튼 팝 애니메이션 + 서버로 `reaction` 이벤트 전송(클라이언트 250ms 연타 제한). 서버(server.js)는 투표(voting) 중 차단, 타입 검증(heart/thumbs만), 소켓당 3초에 10회 스팸 제한 후 전체 broadcast. LED(led.html)는 `#reaction-layer`(z-index 25, 채팅/투표 위)에서 랜덤 위치(가로 8~88%, 세로 25~85%)·랜덤 크기(28~54px) 이모지가 좌우로 살짝 흔들리며 위로 떠오르다 사라지는 애니메이션(Web Animations API, 1.8~2.5초), 동시 표시 80개 상한. socket.io-client 통합 테스트로 정상 수신/타입 검증/스팸 제한/투표 중 차단 확인.
  - 한글 줄바꿈 개선: 질문 텍스트가 글자 단위로 잘리던 문제("만족하십니 / 까?") — 질문 표시 영역(chat.html `#voting-question-phone`, led.html `#voting-question`)과 채팅 말풍선(양쪽) 모두 `word-break: keep-all` + `overflow-wrap: break-word`로 변경해 어절 단위 자연스러운 줄바꿈 적용.
  - LED 투표/결과 화면 레이아웃 조정: 질문(최상단)과 결과 %(최하단)가 공 클러스터와 너무 떨어져 있던 문제 — `#voting-layer`를 flex 컬럼에서 절대배치로 바꾸고, 질문은 `top: 15%`, 결과 배너는 `bottom: 18%`로 공 클러스터(세로 중앙) 위아래 가까이 배치. 캔버스는 `inset: 0` 전체 영역 유지라 공 애니메이션과는 겹치지 않음. voting 모드의 질문+타이머 위치도 동일하게 내려와 전체적으로 중앙 집중 구성.
  - 결과 모드 붐업/붐따 반응 추가: 투표 종료 후 결과 화면이 떠 있는 동안(채팅 복귀 전) 관객 폰 결과 카드에 "이 결과, 어떻게 생각하세요?" 힌트 + 👍/👎 버튼 표시(`#result-react`, result 모드에서만 활성). 누르면 기존 reaction 파이프라인으로 LED 결과 화면 랜덤 위치에 이모지가 떠오름. 서버 reaction 타입에 'down' 추가(허용: heart/thumbs/down, 투표 중 차단·스팸 제한은 동일 적용). LED는 `REACTION_EMOJI` 맵으로 3종 렌더. `sendReaction(type, btnEl)`로 리팩터링해 누른 버튼에 팝 효과. 통합 테스트: 투표 중 차단, result 모드에서 thumbs/down 수신, invalid 타입 거부 확인.
  - 채팅↔질문 모드 전환 LED 애니메이션(미결 사항 해소): "깊이감 전환" 방식 — 질문 모드 진입 시 채팅 레이어가 블러+`scale(0.94)`로 뒤로 물러나고, 질문+타이머(`#voting-question-wrap`)가 관객 폰 투표 카드와 동일한 팝인 바운스(`popIn`, scale 0.4→1.05→1, 0.5s)로 등장. 결과 % 배너(`#result-banner.active`)도 동일 popIn 적용. 복귀 시 채팅 레이어 transition 역재생. voting→result 전환처럼 레이어가 유지되는 동안엔 질문 애니메이션 재생 안 됨(다음 질문에서만). 디테일 다듬기는 5단계 디자인 때 진행 예정.
  - ✅ 2단계 기능 완료(2026-07-15 사용자 확인) — 남은 것은 커밋/Render 재배포와 실기기(LTE) 테스트
- 🔄 **3단계 구현 중**: 주관식 질문 + 흘려보내기 + 워드클라우드 (2026-07-15 코드 완료)
  - 결정사항: 표시 방식 = 접수 중 흘려보내기 + 마감 후 워드클라우드 / 답변 = 1인 1회·20자 이내 / 종료 = 제작진 수동 마감(타이머 없음)
  - 서버(server.js): mode에 `subjective`/`subjectiveResult` 추가. `answers`(clientId→text, 1인 1회 — 중복 제출 거부, 20자 초과 서버에서 자름). `admin:startSubjective`/`admin:endSubjective` 이벤트. `answer` 수신 시 `subjectiveAnswer`{text, answerCount} 전체 broadcast(익명, 텍스트만). `computeCloud()`: 공백 토큰화+특수문자 제거+빈도 집계, 상위 40개 — subjectiveResult 모드의 publicState에 `cloud` 포함. 리액션은 subjective 중에도 차단(voting과 동일 원칙), subjectiveResult에선 붐업/붐따 허용.
  - 관객 폰(chat.html): subjective 모드에서 질문+20자 입력창+보내기 버튼(엔터 제출 가능). 제출 후 "답변이 접수됐어요" 완료 상태로 전환(재제출 불가), 재접속 시 `myAnswered`로 서버 기준 복원. subjectiveResult에선 기존 붐업/붐따 버튼 재사용. `applyVoteState`를 모드별 분기(isYesNo에 따라 YES/NO 버튼·비율 바 표시 여부)로 리팩터링.
  - LED(led.html): subjective 모드에서 답변이 오른쪽→왼쪽으로 흘러가는 말풍선 애니메이션(`#answer-stream`, 랜덤 높이 16~82%·랜덤 크기 18~30px·9~14초 통과, 동시 60개 상한). 마감 시 `#cloud-wrap`에 워드클라우드 popIn 등장 — 빈도 비례 20~66px, 6색 팔레트 랜덤, 큰 단어 몰림 방지 셔플, 답변 0개면 "접수된 답변이 없습니다". 주관식 모드에선 공 캔버스 정지+잔상 클리어.
  - 관리자(admin.html): "주관식 질문 시작"/"주관식 답변 마감(워드클라우드 표시)" 버튼, 주관식 답변 수 실시간 표시, 모드 배지(subjective 파랑/subjectiveResult 보라). 사전등록 질문에 객관식/주관식 타입 선택 추가(기존 문자열 프리셋 자동 마이그레이션), 실행 시 타입에 맞는 이벤트 발송.
  - 통합 테스트 통과: 시작→답변 3건(20자 초과 잘림, 중복 제출 거부)→마감→클라우드 집계("야근" 3회 등)→답변 수→채팅 복귀.
  - 흘려보내기 대기열+레인 개선: 폭주 대비 — 답변을 즉시 띄우지 않고 큐에 쌓아 일정 간격 배출(기본 420ms, 대기 25개↑ 250ms, 60개↑ 150ms 자동 가속, 큐 상한 200). 가로 8개 레인(14~82% 균등 분할, 레인당 3.5초 쿨다운)에 배정해 겹침 방지. 모드 전환 시 큐/타이머 정리(`clearAnswerStream`).
  - 제작진 픽(Pick) 기능: 방송 작가 큐레이션 패턴 — 관리자 화면에 주관식 답변 전체 목록 실시간 표시(스크롤, 새 답변 자동 따라가기), [픽] 버튼 누르면 LED 중앙에 스포트라이트 카드(검정 배경, 30px, popIn, 8초 유지, 연속 픽 시 즉시 교체)로 크게 표시 → 진행자/패널이 보고 토크. 접수 중+마감 후(워드클라우드 위) 모두 픽 가능, 픽된 답변은 목록에 초록 표시('픽됨 · 다시' 재픽 가능). 서버: `answerList`(id 부여)/`pickedAnswers`, `admin:getAnswers`/`admin:pickAnswer`/`answerPicked` 이벤트, chat 모드에선 픽 거부. 통합 테스트: 목록 조회, 픽 broadcast, 없는 id 무시, 채팅 모드 픽 거부, 마감 후 픽, 픽 상태 저장 확인.
  - 답변 폭주 대비 2단계 큐레이션(별표+검색): 답변 200건급 상황에서 제작진이 다 못 훑는 우려 대응 — 라디오 작가 워크플로 차용. 관리자 답변 목록에 ☆ 별표(후보) 버튼(LED 노출 없음, 부담 없는 1차 선별), "☆ 후보만" 필터 토글, 키워드 검색 추가. 별표는 서버(`starredAnswers`, `admin:toggleStar`/`answerStarred` broadcast)로 모든 관리자 화면에 실시간 공유 → 작가 여러 명이 관리자 화면을 나눠 열고 분업 가능(1차: 훑으며 별표 → 2차: PD가 후보만 보기에서 최종 픽). 통합 테스트: 별표 토글 공유, 해제, 재접속 동기화 확인.
  - 워드클라우드 연출 개선(사용자 선택: 크기 대비 강화 + 등장 연출): 크기를 18~92px로 확대하고 지수 스케일(`scale^1.6`)을 적용해 1등 단어가 압도적으로 크게 보이도록 변경(예: 빈도 12/5/3/1 → 92/33/23/18px). 등장 연출 — 빈도 낮은 단어부터 하나씩 팝인(단어 수에 따라 80~250ms 간격 자동 조절, 전체 ~4초), 1등 단어만 600ms 뜸 들인 뒤 마지막에 scale 1.3 오버슈트로 크게 등장(`wordInTop` 키프레임, `.cloud-word.top`). 애니메이션은 CSS `animationDelay` 기반.
  - 워드클라우드 글자 뭉개짐 버그 수정: `font-weight: 800`이 한글 시스템 폰트(Apple SD Gothic Neo)에 없는 굵기라 브라우저가 가짜 굵기(이중 렌더)로 흉내 → "빼" 같은 획 많은 글자가 뭉개져 보임. 워드클라우드·스포트라이트 카드 모두 700으로 변경(+line-height 1.2). 참고: 향후 한글에 font-weight 800 지정 금지.
  - 워드클라우드 집계 방식 변경(단어→문구 전체): "나 빼고 다"가 "빼고/다/나"로 쪼개져 뒤섞이는 문제 — 20자 단답형 특성상 답변 전체를 한 덩어리로 집계하도록 `computeCloud()` 변경(공백만 정규화, 같은 답변끼리 합산). 어순/의미 보존, 동일 답변 많을수록 그 문구가 커짐. 트레이드오프: "야근"과 "야근 힘듦"은 별개 집계 — 유사 답변 묶기(형태소/유사도)는 5단계 검토 항목으로 유지. 테스트: "나 빼고 다" 3명(공백 변형 포함 병합 확인) count 3 정상.
  - 워드클라우드 색상 개선: 6색 랜덤 배정(인접 중복 가능) → 10색 팔레트 순환 배정(`i % length`)으로 변경, 이웃 답변끼리 항상 다른 색 보장. 답변 10개 이하면 전부 다른 색.
  - 워드클라우드 흩뿌림 배치: 답변이 적을 때 한 줄로 이어져 한 사람 답변처럼 보이는 문제 — 답변마다 랜덤 오프셋(가로 ±15px, 세로 ±45px)과 기울기(±5도, 1등 단어는 0도)를 CSS 변수(--dx/--dy/--rot)로 지정, 등장 애니메이션 keyframes에 translate/rotate 포함(transform 충돌 방지). 서로 다른 사람의 답변으로 인지되도록 개선.
  - 모바일 레이아웃 버그 수정: 주관식 입력창(`#answer-input`)이 input 기본 최소 너비(~200px) 때문에 좁은 화면에서 줄어들지 못해 보내기 버튼이 카드 밖으로 밀리는 문제 — `min-width: 0` 추가. 같은 유형 예방: `#msg-input`(채팅 입력), admin `.add-row input`/`.answer-toolbar input`에도 동일 적용. (교훈: flex 컨테이너 안의 input/textarea에는 min-width: 0 필수)
  - 관리자 답변 목록 전체화면 모드: 답변이 많아지면 운영 카드들 사이에서 스크롤 과다 문제 — 답변 목록 카드 헤더에 [전체화면] 토글 버튼 추가. 누르면 해당 카드만 화면 전체(fixed inset 0, z-100)를 차지하고 목록이 남은 높이를 다 씀(검색/후보만 필터 포함), [닫기]로 복귀. 작가용 전용 화면으로 사용 가능(운영은 다른 탭/사람이 담당).
  - 채팅 핀 고정 기능(사용자 제안): 채팅 모드에서 진행자/패널이 특정 채팅을 언급할 때 그 채팅을 LED 중앙에 고정 — 관리자 화면의 "LED 채팅창 제어" 카드를 "실시간 채팅" 카드로 개편(실시간 피드, 스크롤/클릭 가능, 전체화면 토글, 멈추기 버튼은 카드 안에 유지). 채팅 클릭 → `admin:pinChat` → LED 중앙 팝업(닉네임+내용, 배경 rgba(255,255,255,0.55) 반투명 — 뒤에서 채팅 계속 흐름, z-index 15). 해제는 수동([핀 내리기]) 또는 다른 채팅 클릭 시 교체(주관식 스포트라이트의 8초 자동 소멸과 달리 MC 토크 동안 유지 목적). 서버 `pinnedChat` 상태로 재접속 복원, 질문 모드 진입/복귀 시 자동 해제. 테스트: 고정/교체/투표 시 자동 해제/투표 중 차단/불필요 broadcast 없음 확인.
  - 관리자 화면 2열 배치: 900px 이상 화면에서 왼쪽 열 = 운영/제어(현재 상태, 질문 준비, 즉석 질문, 진행 제어), 오른쪽 열 = 모니터링(실시간 채팅, 주관식 답변 목록 — 스크롤 영역, max-height 46vh로 확대). 899px 이하(태블릿 세로/폰)는 기존 1열 자동 복귀. HTML을 `.admin-grid > .admin-col` 2개 구조로 재배치.
  - 관리자 전체화면 버튼 스타일 통일: 채팅 카드 버튼이 스타일 미지정으로 브라우저 기본 모양이던 문제 — 공용 클래스 `.expand-btn`으로 통합(주관식 답변 목록 버튼 디자인 기준). **디자인 원칙(사용자 요청): 같은 역할의 UI 요소는 공용 클래스로 만들어 일관성 유지 — 개별 id 스타일 지정 금지.**
  - ✅ 코드/커밋 완료: 3단계 전체(주관식+흘려보내기+워드클라우드+픽+별표+채팅 핀)가 커밋 `dfcad6e`에 반영됨. 2026-08-15 헤드리스 통합 테스트 27개 항목 전부 통과(주관식 시작→답변 접수·20자 잘림·1인1회 거부→흘려보내기 broadcast→주관식 중 리액션 차단→답변목록/별표/픽→마감 후 워드클라우드 문구 병합→결과 모드 붐업/붐따 허용→채팅 복귀→채팅 핀 고정/교체/해제·주관식 중 차단).
  - ⬜ 남은 작업: 시각 확인만 남음(흘려보내기 애니메이션/워드클라우드 연출/스포트라이트/핀 팝업) — Claude 브라우저 확장 연결 필요. 로직은 검증 완료. Render 재배포는 사용자 환경에서 진행.
- 🔄 4단계: 제작진 관리 화면 + 신규 질문 유형 (2026-08-15 실제 방송용 질문 리스트 입수, 실현성/배치 판단 완료 — 개발 착수 전)
  - 실사용 질문 출처: `file/AI 실증 실험 질문 리스트 (0810ver).pdf` — 울산 공연(어반자카파/펀치/치즈) 관객 대상 9문항. 유형별: Yes/No 3개([1]라인업 앎/[4]울산시민/[6]날씨), 객관식 N지선다 3개([2]응원대상 4지/[3]동행 4지/[7]기대되는것 4지), 주관식 2개([5]출신지역/[8]가수에게 다섯글자), 이모티콘 선택 1개([9]지금기분 4이모지).
  - 현재 지원(사전등록만 하면 실행 가능): Yes/No 3개 + 주관식 2개 = 5문항.
  - 신규 개발 필요 = 3가지 기능:
    1. **N지선다 객관식**([2][3][7]): 투표 상태를 yes/no 하드코딩 → 옵션 인덱스(2~4개)로 일반화(Yes/No는 옵션 2개 특수케이스로 하위호환). 서버 votes 일반화, LED N개 공 클러스터(색/라벨/배치 확장, 합체 애니메이션은 클러스터별 재사용), 관객 폰 N버튼+비율 바 N줄, 관리자 보기 입력칸.
    2. **이모지 반응 질문**([9]): 사용자 결정=떠오르는 이모지 연출(비율 X). 관객이 이모지 4개 중 선택 → LED에 그 이모지가 리액션처럼 대량으로 떠오름(기존 reaction 파이프라인 확장). '분위기 고조'용, 공연 시작 직전 성격에 부합.
    3. **질문별 글자수 설정**([8] 다섯글자): 사용자 결정=주관식 시작 시 최대 글자수 지정(현재 전역 20자). 서버 startSubjective가 maxLength 받고, 관객 입력창/서버 슬라이스/관리자에 반영. 짧을수록 워드클라우드 뭉침도 개선.
  - 조건부 분기([4] 울산시민 아니면 [5]만): 사용자 결정=자동 분기 개발 안 함, 제작진이 앞 결과 보고 다음 질문 수동 실행(현 수동 구조와 일치).
  - 관리자 배치 방침: 새 카드 신설 X. 기존 "질문 준비"·"즉석 질문" 카드 확장 — 타입 드롭다운을 [Yes/No 투표 · 객관식(선택지) · 주관식 · 이모지 반응] 4종으로, 객관식/이모지 선택 시 보기 입력칸(2~4개) 펼침, 주관식은 글자수 입력 노출. 프리셋 저장 구조에 options[]/maxLen 추가(기존 마이그레이션 로직 재사용). 배지색은 기존 .preset-tag 팔레트에서 유형별 배정. (디자인 일관성 원칙 준수)
  - 임시 admin.html은 2단계에서 선반영됨 — 위 확장 + 정식 디자인 보강이 4단계 범위.
  - ✅ 2026-08-15 코드 구현 완료 (신규 3기능):
    - **서버(server.js)**: 투표를 options 기반으로 일반화 — `voteType`('yesno'|'choice')/`voteOptions[]` 추가, `votes`를 clientId→보기 인덱스(0-based)로, `computeCounts()`가 `{perOption:[], total}` 반환. `publicState`에 voteType/voteOptions/answerMaxLen/emojiOptions 추가, myVote는 인덱스(0 falsy 주의—null 체크). `startVoting(q,options,duration,type)`, `startSubjective(q,maxLen)`, 신규 `startEmoji(q,emojis)`+mode 'emoji'. `admin:startVoting`(type/options)·`admin:startSubjective`(maxLen)·신규 `admin:startEmoji`. `vote`는 인덱스 검증, 신규 `emojiPick`→`emojiFloat` broadcast(여러 번 탭 허용, 3초 10회 스팸제한). reaction은 emoji 모드에서도 차단. maxLen 상한 20(MAX_ANSWER_LENGTH), 보기/이모지 최대 6개·2개 미만 무시.
    - **관객 폰(chat.html)**: 보기 버튼/비율을 state.voteOptions로 동적 생성(색은 CSS변수 --tint/--strong/--on 주입, YESNO_STYLES/OPTION_STYLES 팔레트, 3지↑ 2열 grid). 이모지 반응 버튼(#emoji-buttons, 탭 시 emojiPick+팝). 주관식 입력 maxlength/placeholder를 answerMaxLen로. castVote(인덱스), updateVoteUI/updateRatio 일반화. myVote 인덱스 0 하이라이트 버그 방지(`!=null` 체크). font-weight 800→700(한글 보기 라벨 대비).
    - **LED(led.html)**: 공 클러스터를 2개(yes/no) 고정 → N개 동적. tierState 배열화, clusterCenter 격자배치(2지=좌우/3지=가로3/4지=2×2/…), 보기 수에 따라 curScale로 공 크기 축소(겹침 방지), 보기별 색(colorFor, 폰과 순서 동일), 라벨은 공 무리 위(extent 기준). result 배너 N개 색상 세그먼트. voteUpdate/applyState가 perOption 사용. 신규 emoji 모드=공/타이머/배너 없음, `emojiFloat`로 이모지 부유(reaction과 spawnFloat 공용). ⚠️ canvas 의존이라 jsdom 테스트 제외 — 서버 데이터 계약+문법으로 커버.
    - **관리자(admin.html)**: 질문준비·즉석질문 카드에 유형 4종(YES/NO·객관식(선택지)·주관식·이모지 반응) 드롭다운, 유형별 보조입력 토글(보기 /구분·이모지 공백구분·글자수). emitStart 공용 함수로 즉석/사전등록 분기, 프리셋에 options/emojis/maxLen 저장. 집계표시(status-counts) perOption 대응, 모드배지 emoji 추가, 태그색 choice/emoji 추가. 종료버튼 문구 "선택형 투표 종료".
    - **테스트**: 서버 통합 50개(3단계 회귀 27 + 4단계 신규 23), jsdom DOM 테스트 chat 18 + admin 19 = 총 87개 전부 통과(포트 4000). 검증 내용: N지선다 인덱스 투표·집계·답변변경·범위검증, Yes/No 하위호환, 질문별 글자수 잘림, 이모지 연속탭·보기밖 거부·모드 중 리액션차단, 관객폰 동적 버튼/비율/이모지 렌더·myVote=0 하이라이트, 관리자 유형토글·페이로드·프리셋 저장/실행.
  - ✅ 2026-08-15 브라우저 시각 검증 완료(Claude in Chrome 확장, 헤드리스 소켓 클라이언트로 투표/이모지 생성): 관리자 유형 드롭다운+조건부 보기입력 노출, LED N지선다 4클러스터 2×2 격자·보기별 색(초록/주황/파랑/보라)·결과 % 색상배너, LED 이모지 부유 연출, 관객폰 4버튼 2×2·선택 하이라이트·색상 비율 바, 관객폰 이모지 4버튼 모두 정상 확인. 스크린샷 근거 확보.
  - ✅ 2026-08-15 커밋(`e1dd714`)+GitHub 푸시+Render 재배포 완료. 배포본(https://chat-app-6kl5.onrender.com)에 신규 코드 반영 확인(관리자 "이모지 반응"·"객관식(선택지)" 마커, 세 화면 200). Render 서비스: Yeomtang/chat-app, branch main, Service ID srv-d8od55urnols73ctnu2g. **Auto-Deploy = "On Commit"(자동배포 켜짐)** — main 푸시하면 자동 배포됨. 단 무료 플랜은 빌드 큐 대기+슬립 인스턴스 스핀업으로 반영이 수 분(~5-10분) 걸릴 수 있으니 푸시 직후 배포본을 폴링해도 한동안 이전 버전이 보일 수 있음(정상). 대시보드 Logs에서 빌드 진행 확인 가능.
  - ⬜ 남은 작업: 실기기(LTE) 테스트. 정식 4단계 관리자 디자인 보강(현 admin.html은 임시 컨트롤 기반).
- ⬜ 5단계: 디자인 디테일 + LED 최적화
  - ⚠️ **5단계 시작 시 사용자에게 상기시킬 것**: 금칙어 필터 추가 (욕설/부적절 답변·채팅 자동 차단 — 채팅, 주관식 답변 양쪽. 12월 실방송 전 필수)
  - 채팅↔질문 전환 애니메이션 디테일 다듬기(깊이감 전환 속도/scale 정도), 스포트라이트 유지 시간(현 8초)/카드 스타일, 워드클라우드 형태소 분석(조사 제거) 검토
  - **채팅 핀 팝업 디자인 개선**: 기능은 확정(2026-07-15), 현재 디자인(흰 카드+반투명 배경)은 임시 — 사용자가 레퍼런스 찾아서 수정 방향 제시 예정

## 일정
- **7월 말**: 테스트 버전 목표 (디자인 검토 및 수정 가능한 수준)
- **12월**: 실제 방송 사용
- 개발 방식: 단계적 진행, 각 단계마다 확인 후 수정

## 결정사항 히스토리
- 2026-08-15: 이모지 반응 질문 기본 보기 고정 — 관리자에서 '이모지 반응' 유형 선택 시 보기 입력창에 공연 [9] 공식 세트("🔥 소리 질러 / 🥹 벌써 눈물 남 / 😱 너무 떨려요 / 🤩 빨리 보고 싶어요")가 자동으로 채워짐(DEFAULT_EMOJI_OPTIONS 상수, 즉석·사전등록 both). 필요시 수정 가능.
- 2026-08-15: 사용자 피드백 UI 수정 5건(관리자 2 + LED 핀 3). ① 관리자 즉석질문 유형셀렉트↔보기입력 간격 — 보기 입력 위에 라벨(question-options-label, 유형따라 "보기 (선택지)"/"이모지 + 뜻") 추가해 다른 필드와 동일 간격. ② 질문 유형 변경 시 이전 유형 보조입력값(보기/글자수) 초기화(즉석·사전등록 both, 질문 텍스트는 유지). ③ 사전등록 목록 겹침 개선 — .preset-item을 .preset-main(질문 윗줄 + 보기/설정 회색 detail 아랫줄) + .preset-actions(실행/✕ 우측 묶음) 2단 구조로 재구성. ④ LED 채팅 핀 박스 폭을 내용맞춤→채팅폭 고정(width 86%)하고 슬림화(padding 8px, 본문 20px, 닉 12px, radius 16px — "길쭉한 바"). ⑤ 핀 본문 긴 메시지 줄바꿈을 text-wrap: balance로 줄 길이 고르게 정리(가독성). 테스트 95개 통과.
- 2026-08-15: 관객 폰 투표 화면에서 실시간 비율(%)을 투표 중엔 숨기고 결과 모드에서만 공개하도록 변경(사용자 요청, 결과가 궁금하도록 = LED와 동일 원칙). chat.html applyVoteState의 ratio 표시/갱신을 `mode==='result'`로 한정. N지선다·Yes/No 모두 적용(2단계의 'Yes/No 실시간 비율' 결정을 이번에 뒤집음 — 필요시 Yes/No만 되돌릴 수 있음). 커밋 30bc8e3.
- 2026-08-15: 이모지 뜻 라벨 초기 버전에서 파싱 버그 발견/수정 — 이모지와 뜻을 붙여 쓰면("🔥소리질러") 첫 공백까지를 이모지로 잡아 "🔥소리질러"가 통째로 큰 아이콘이 돼 뭉개짐. parseEmojiOptions를 `\p{Extended_Pictographic}`(+VS16/ZWJ/스킨톤) 유니코드 정규식으로 바꿔 공백 유무와 무관하게 선행 이모지만 분리. 관객 폰 .emoji-btn도 정돈(min-height 104px 균등, 아이콘 36px/라벨 14px 600, justify-content center). 붙여쓰기/공백 케이스 DOM 테스트 추가. (교훈: 이모지 파싱은 공백 가정 금지, 유니코드 속성으로 분리)
- 2026-08-15: 이모지 반응 질문([9])에 뜻 라벨 추가 — 관객이 이모지 의미를 알도록 각 보기를 {emoji,label}로. 관객 폰 버튼에 이모지(38px)+뜻(13px 회색) 세로 표시(.emoji-btn-icon/.emoji-btn-label). LED 부유는 이모지만 유지(분위기 연출). 관리자 입력은 "/"로 보기 구분+공백으로 이모지/뜻 구분(예: "🔥 소리 질러 / 🥹 벌써 눈물 남"), parseEmojiOptions로 파싱. 서버 emojiPick 검증도 o.emoji 기준. 사용자가 실제 써보고 판단 예정(우선 PDF 예시대로 노출). 테스트 91개(서버 50+DOM 41) 통과.
- 2026-08-15: 4단계 신규 3기능(N지선다 객관식/이모지 반응/질문별 글자수) 코드 구현 완료. 투표 상태를 options+인덱스 기반으로 일반화(Yes/No 하위호환 유지), 관리자에 유형 4종 드롭다운. 서버 통합 50 + jsdom DOM 37 = 87개 테스트 통과. 시각 확인은 브라우저 확장 연결 후로 유예, 미커밋. 상세는 위 4단계 항목.
- 2026-08-15: 실사용 질문 리스트(울산 공연, PDF 9문항) 입수 → 실현성/관리자 배치 공동 판단. 신규 개발 3종 확정(N지선다 객관식 / 이모지 반응=떠오르는 연출 / 질문별 글자수 설정), 조건부 분기는 운영 수동 처리, 관리자는 기존 질문준비·즉석질문 카드 확장(타입 4종). 개발 착수 전 단계 — 4단계로 진행 예정. 상세는 위 4단계 항목 참조.
- 2026-08-15: 3단계 로직 헤드리스 통합 테스트(포트 4000) 27개 항목 전부 통과로 검증 완료. ⚠️ 기록 정정: memory.md에 "오늘 작업 전체가 미커밋"이라 적혀 있었으나 실제로는 3단계 전체가 커밋 `dfcad6e`에 이미 반영된 상태였음(미커밋은 memory.md 1줄·신규 CLAUDE.md·임시 테스트 파일뿐). 임시 테스트 스크립트 `__livetest*.js`는 .gitignore에 추가. CLAUDE.md(디자인 일관성 원칙) 커밋. 시각 확인은 브라우저 확장 연결 후로 유예.
- 2026-07-15(추가): 채팅 핀 고정 기능 구현(관리자 실시간 채팅 뷰에서 클릭 → LED 반투명 팝업, 수동 해제). 핀 팝업 디자인은 임시 — 사용자가 레퍼런스 준비 예정. 관리자 화면 2열 배치(운영/모니터링 분리), 전체화면 버튼 공용 클래스 통일, CLAUDE.md에 디자인 일관성 원칙 6개 추가. 버튼 문구 정리("질문 준비" 순서 문구 제거, "객관식(YES/NO) 투표 종료"로 대칭). ⚠️ 오늘 작업 전체가 미커밋 상태 — 다음 세션에서 커밋/Render 배포 여부 확인 필요.
- 2026-07-15: 3단계 주관식 설계 확정 — 접수 중 흘려보내기+마감 후 워드클라우드, 1인 1회 20자, 제작진 수동 마감. 코드 구현 및 통합 테스트 완료.
- 2026-07-15: 2단계 추가 개선 — 결과 화면 질문 유지, LED 채팅 40개 표시, 채팅 100자 제한, 리액션(❤️/👍 채팅 중, 👍/👎 결과 화면), 한글 어절 줄바꿈, LED 질문/결과 위치 조정(top 15%/bottom 18%), 채팅↔질문 전환 깊이감 애니메이션.
- 2026-06-15: 초기 프로젝트 맥락 수집
- 2026-06-15: 화면 비율(2:3), 채팅/질문 듀얼 모드 방향 확정
- 2026-06-16: 2단계(Yes/No 투표) 구현 — 서버 상태머신, 공 애니메이션(Canvas), 타이머, 임시 관리자 패널까지 코드 작성 완료. 디자인은 5단계로 유예 방침 유지(현재는 기능 동작 위주).
- 2026-06-17: LED 공/라벨 1.5배 확대, 관객 폰 투표 박스 등장 모션(중앙에서 커지는 효과) 추가. 아직 git 커밋/푸시 전 — 사용자가 본인 환경에서 lock 해제 후 커밋 필요.
- 2026-06-17: gemini.md 기준 Gemini 작업 3건(2단계 구현, 채팅 히스토리 버그 수정, 닉네임 클라이언트사이드 최적화) 코드 검증 완료 — HEAD와 정확히 일치, 로컬 미커밋 변경과 충돌 없음. 검증 중 닉네임 최적화가 동시접속자 중복 가능성을 만든 것을 발견해 서버 측 중복 감지/교체 로직(`claimNickname`/`nicknameReassigned`) 추가로 수정. 샌드박스 쪽 git lock은 구조적 권한 제한(마운트 폴더에서 unlink 자체가 막혀 있음)으로 확인 — 커밋/푸시는 계속 사용자 환경(Gemini 등)에서 진행 필요.
- 2026-06-18: gemini.md → memory.md 통합(Gemini 작업) 검증 완료, 누락/충돌 없음, 깨진 참조 없음. 미투표 상태에서 "NO 100%" 오표시 버그 발견·수정(chat.html, led.html).
- 2026-06-18: LED 송출 화면 투표 중 실시간성 보완 시도 → 숫자 배너 추가했다가 사용자 요청으로 되돌림. 대신 기존 공 클러스터 이동 애니메이션이 카운트다운 중 실시간 현황(사람들이 어느 쪽으로 움직이는지)을 보여주는 역할로 확정.
- 2026-06-18: LED 결과 화면에 재미 요소로 공 합체 효과 추가 — 1표 공 10개→10표 단위 공, 10표 단위 공 10개→100표 단위 공으로 단계적으로 커지는 표현. 투표자별 개별 공 추적 방식을 버리고 서버 집계 카운트 기반 단계(tier) 시스템으로 전환. 단계 계산/패킹 로직 Node 테스트로 검증 완료.
- 2026-06-18: LED 채팅 화면에 일시정지 기능 추가 — 채팅 폭주 시 진행자/패널이 읽을 시간 부족 문제 해결을 위해, 사용자와 4가지 방향(속도제한/샘플링/카드/조합) 논의 후 사용자가 직접 제안한 "LED 화면 클릭=정지, 더블클릭=재생" 방식으로 결정. 재생 시 밀린 메시지를 따라잡지 않고 최신으로 점프, 정지 상태 시각 표시는 넣지 않기로 확정(둘 다 사용자 선택).
- 2026-07-15: 결과 화면에 질문 텍스트 유지하도록 변경 — 타이머 종료 후 공 클러스터·%만 남아 무엇에 대한 결과인지 알 수 없던 문제. LED(led.html)·관객 폰(chat.html) 모두 result 모드에서 타이머만 숨기고 질문은 상단에 계속 표시.
- 2026-06-18: 위 일시정지 기능의 제어 방식 변경 — LED 화면 직접 클릭 방식은 현실적으로 동작하지 않을 것으로 판단되어 폐기. 관리자 화면(admin.html)에 정지/재생 버튼을 추가하고 서버(`appState.chatPaused`, `admin:pauseChat`/`admin:resumeChat`, `chatPauseChange` broadcast)를 거치는 방식으로 전환. 정지 중에도 관객 채팅 송수신은 영향받지 않도록 서버 로직 분리 확인(통합 테스트로 검증).
- 2026-08-21: **전체 디자인 리뉴얼 — 블러시 팔레트 적용** (사용자 제공 시안 이미지 기준). 적용 범위는 LED(led.html) + 관객 폰(chat.html) 2개 화면 (admin.html은 내부 화면이라 제외 — 사용자 선택).
  - **디자인 토큰화**: 두 파일 `:root`에 동일한 CSS 변수 팔레트 정의. `--bg #efdcd5`(블러시 배경) / `--bg-soft #f8ece7` / `--surface #ffffff` / `--ink #1e1e42`(네이비) / `--ink-soft #6e6a86` / `--ink-faint #a49eb4` / `--orange #fb7e55` / `--purple #5b5cc4` / `--warn #d94f3d`. 기존 iOS 계열 하드코딩 색(#1c1c1e, #f2f2f7, #34c759, #ff3b30 등)은 전부 변수로 치환. **두 파일의 :root 값은 반드시 동일하게 유지할 것.**
  - **폰트**: Pretendard Variable (jsDelivr CDN, dynamic-subset — 한글 서브셋만 받아 LTE 부담 적음)로 교체. font-weight는 여전히 700 상한 유지.
  - **LED 질문 카드**: 질문을 흰 카드(`#voting-question-card`)로 감싸고, 기존 타이머 원을 카드 상단 중앙에 걸치는 배지로 변경(시안의 넘버 배지 형태). result 모드에선 배지만 사라지고 카드는 유지.
  - **LED 보기 상자(핵심 구조 변경)**: 자유롭게 떠 있던 공 클러스터를 시안대로 "컬러 헤더 + 흰 본문" 상자 안에 담는 방식으로 변경. canvas에 `BOX` 상수 + `roundRectPath()` / `boxRect()` / `drawBox()` 추가. 보기 라벨은 클러스터 위 텍스트 → 상자 헤더 안으로 이동. 표가 쌓여 무리가 커지면 셀 한도까지 상자도 커지고, 한도를 넘으면 `fit` 배율로 공을 축소해 상자 안에 담는다(잘림 방지). 2지선다·4지선다(2열 그리드) 모두 스크린샷으로 확인.
  - `#voting-canvas-wrap`을 `inset: 0` → `top: 24%; bottom: 12%`로 변경. 그렇지 않으면 2행 레이아웃(4~6지선다)에서 상단 상자가 질문 카드와 겹침.
  - **버그 수정(기존 버그)**: `resizeCanvas()`가 공 배치(`applyCounts`) 이후에 실행돼, 새로고침/재접속 시 공이 기본 캔버스 크기(300x150) 기준 좌표에 배치돼 화면 좌상단에 박혀 있었음. `applyState`에서 배치 전에 `resizeCanvas()`를 먼저 호출하고, `resizeCanvas()` 안에서 모든 클러스터를 `repackSide()`로 재배치하도록 수정. 상자 클리핑을 넣으면서 드러난 문제.
  - **관객 폰 보기 버튼**: 단색 버튼 → 시안과 같은 "컬러 헤더 + 본문" 카드(`.vote-btn-head` / `.vote-btn-body`). 본문은 투표 중 선택 표시(✓), 결과 모드에서만 비율(%) 표시. 선택 시 컬러 링(box-shadow). 결과 모드에서 `:disabled` dim을 제거(비율이 흐려져 안 읽히는 문제).
  - **주의(회귀 방지)**: `updateRatio()`가 보기 카드 본문에도 %를 쓰므로 반드시 `currentMode === 'result'` 가드를 유지할 것. 없으면 투표 중에 비율이 노출됨(기존 "투표 중 % 숨김" 정책 위반).
  - **강조 위계 3단계**: 일반 채팅 말풍선 = 흰색, 채팅 핀(`#chat-pin-card`) = 오렌지+흰 글씨, 제작진 픽 스포트라이트(`#spotlight-card`) = 네이비+흰 글씨.
  - 워드클라우드 팔레트(`CLOUD_COLORS`)도 블러시 톤에 맞게 교체.
  - 검증: headless Chrome + CDP로 채팅/투표(2지·4지)/결과/주관식/워드클라우드/이모지/채팅핀 전 모드 스크린샷 확인. 검증용 임시 스크립트(`__shot.js` 등)는 작업 후 삭제함.
- 2026-08-21: **LED 투표 시각화 재설계 — 1표 = 공 1개, 상자 바닥부터 쌓기** (103:243 테스트 화면을 보다가 문제가 드러나 전면 교체).
  - **발견한 문제 (3자리 수 규모에서 드러남)**: (1) 상자가 표 수에 비례해 커지도록 만들어놔서 표 많은 쪽 상자가 옆 상자를 **덮어버림**, (2) 보기마다 상자 크기·높이가 달라 레이아웃이 깨진 것처럼 보임, (3) 자릿수 분해(100/10/1단위 공) 방식이라 104표=공 5개 / 243표=공 9개로 나와 **양 차이를 전혀 읽을 수 없었음**.
  - **상자 = 고정 격자로 변경**: `boxRect()`가 더 이상 `tierState[i].extent`를 참조하지 않고, 캔버스 폭에서 좌우 여백(`BOX.marginRatio` 0.11)과 `BOX.gap`을 뺀 값을 `cols`로 균등 분할. 높이는 `bw * BOX.maxAspect`(1.15)로 상한. **모든 보기의 상자 크기가 항상 동일** → 겹침·불균형 원천 차단. `clusterCenter()`는 이제 `boxRect()`에서 파생(상자가 배치의 기준, 공이 따라옴).
  - 상자 격자가 세로로 꽉 차게 되어 `#voting-canvas-wrap`을 `top:22%/bottom:22%`로, `#result-banner`를 `bottom:11%`로 조정(2행 레이아웃에서 결과 배너와 충돌 방지).
  - **자릿수 tier 시스템(ones/tens/hundreds) 전면 제거**: `BALL_SCALE`/`TIER_SCALE`/`BALL_R`/`packTier`/`syncTierArray`/`drawTier`/`tierState` 삭제. 대신 `ballState[i] = { balls: [...] }`로 1표당 공 1개.
  - **벌집 쌓기**: `hexPositions()`가 상자 바닥부터 한 줄씩 채워 올림(홀수 줄은 가운데 정렬 덕에 자연스럽게 반 칸 어긋남). **쌓인 높이 = 표 수**라서 비율이 한눈에 읽힘. `BALL_GAP`(1.1)로 공 사이 간격, `HEADROOM`(1.12)으로 1등 보기가 천장에 닿지 않게 여유.
  - **공 크기는 전 보기 공통**: `computeBallR(maxCount)`가 "표가 가장 많은 보기가 상자에 딱 담기는 최대 반지름"을 이분 탐색으로 구함(`BALL_MIN_R` 2.5 ~ `BALL_MAX_R` 15). **보기마다 크기를 따로 정하면 양 비교가 왜곡되므로 반드시 `applyAllCounts(perOption)`로 한 번에 갱신할 것** (개별 `applyCounts(i, n)` 방식은 폐기). 표가 적을 땐 최대 크기를 유지해 차오르는 과정이 보이고, 많아지면 자동 축소.
  - 공 크기가 바뀔 때 툭 튀지 않도록 `curBallR`이 `ballR`로 수렴하며, drawFrame이 매 프레임 `curBallR` 기준으로 재배치.
  - 검증(스크린샷): 3:5(소규모, 최대 크기 유지) / 104:243(30%:70% 높이차 명확) / 4지선다 40·120·75·20(2열 그리드) / 결과 모드 % 배너까지 확인.
  - 실용 상한: 상자당 약 2,000표(BALL_MIN_R 기준). 그 이상은 상자 위로 잘림 — 이번 방송 규모(수백 명)에서는 문제 없음.
- 2026-08-21: **전체 화면 상태별 점검 + 발견한 문제 수정** (headless Chrome + CDP로 14개 상태 × 여러 해상도 스크린샷 검증).
  - **LED 말풍선 라인 정렬** (사용자 요청): `.led-bubble`의 `max-width: 88%` → `100%`. 하단 입력 바와 좌우 패딩이 같은 28px이므로 오른쪽 끝 라인이 정확히 맞음. (참고: `.led-msg-group`이 flex column이라 말풍선은 내용 길이와 무관하게 항상 최대 너비로 늘어난다 — 의도된 동작으로 유지. 관객 폰 `.msg-bubble`도 동일하게 80% 고정 폭이며 이건 손대지 않음.)
  - **LED 해상도 독립화 (중요)**: 폰트·여백이 고정 px라서 600폭 미리보기와 1080폭 실제 LED에서 글자 크기 비율이 35%나 달랐음. `#screen`에 `--u`(= 실제 폭 ÷ 기준 폭 800)를 JS로 설정하고, `<style>` 블록의 **모든 px를 `calc(Npx * var(--u))`로 변환**. 캔버스는 `ctx.scale(dpr * uScale)`로 800px 기준 논리 좌표계를 쓰도록 바꿔(`logicalW()`/`logicalH()`) 상자·공 상수를 그대로 재사용. 600/800/1080에서 질문 폰트가 화면 폭의 2.75%, 카드 폭이 26.9%로 **완전히 동일**함을 확인. **앞으로 led.html에 px를 추가할 땐 반드시 `calc(Npx * var(--u))` 형태로 쓸 것** (%·vw·vh는 그대로 두면 됨).
  - **긴 질문이 보기 상자를 덮던 문제**: 질문이 2줄 이상이면 질문 카드가 `#voting-canvas-wrap`(top 22%)를 침범. `layoutBelowQuestion()` 추가 — 질문 카드의 실제 높이(`offsetHeight`, 팝인 transform 영향 없음)를 재서 캔버스와 워드클라우드의 `top`을 동적으로 내림. applyState와 resize에서 호출.
  - **3지선다 상자가 지나치게 작던 문제**: 세로 비율 상한이 1.15 고정이라 열이 늘수록 상자가 작아지고 화면 세로 공간을 30%만 씀. `maxAspectFor(cols, rows)` 도입 — 1행 2열은 1.15(시안 유지), 1행 3열 이상은 2.2(세로 긴 실린더), 2행 이상은 1.6.
  - **관객 폰 투표 카드 잘림**: 보기 6개 + 긴 질문이면 카드가 뷰포트보다 커져 위아래가 잘리고 스크롤도 안 됐음(`align-items:center` + overflow 조합의 고질적 문제). `#voting-overlay`에서 `align-items:center`를 빼고 `overflow-y:auto` 추가 + `#voting-card`에 `margin:auto`. 320x568 최악 케이스에서 top -13 → +28로 개선, 스크롤 가능 확인.
  - **서버: 닉네임 미검증**: `chat` 핸들러가 클라이언트가 보낸 `data.nickname`을 타입/길이 검사 없이 그대로 브로드캐스트 → LED 레이아웃 파괴 가능. `MAX_NICKNAME_LENGTH = 20`으로 자르고 문자열 타입 검사 추가(실제 닉네임 풀은 최대 12자).
  - **overflow-wrap 누락 3곳** (CLAUDE.md 규칙 4 위반): admin `.chat-item .chat-text`(공백 없는 100자 ㅋㅋㅋ가 박스 밖으로 넘침 — 실제 재현됨), admin `.answer-item span`(+ `min-width:0`), led `.cloud-word`(+ `max-width:100%`).
  - `#chat-header .header-info`에 `min-width: 0` 추가 (규칙 5).
  - **검증한 상태**: 채팅(빈/혼합/45개 폭주/긴닉네임·100자영문·이모지), 채팅 핀, 투표(0표/긴질문/2·3·4·6지선다/420:980 대량), 결과, 주관식, 워드클라우드(정상/빈값), 이모지(2개/6개), 스포트라이트, 일시정지. 해상도: LED 600·800·1080폭, 폰 320·390·430폭. 전 상태 JS 에러 0건.
- 2026-08-21: 관객 폰 결과 화면의 비율 범례(`.ratio-text`)가 왼쪽 정렬이라 카드 안 다른 요소(질문·보기 카드·안내문)와 축이 어긋나 보이던 문제 — `justify-content: center` 추가. 보기 3개 이상이라 범례가 여러 줄로 감길 때도 각 줄이 가운데로 모인다.
- 2026-08-21: 위 "LED 투표 시각화 재설계" 항목 중 일부는 같은 날 후속 작업으로 바뀌었으니 주의 — `BOX.maxAspect`(고정 1.15)는 `maxAspectFor(cols, rows)` 함수로 대체됐고, `#voting-canvas-wrap`의 `top`은 CSS 고정값이 아니라 `layoutBelowQuestion()`이 질문 카드 높이에 맞춰 런타임에 설정한다.
- 2026-08-24: **행사별 테마 시스템 추가 — 8/29 공연장 현장 테스트용 관객 입장 주소 `/concert`**.
  - 배경: 12월 본 공연은 "직장 썰루션 토크콘서트"(직장인 테마)인데, 8/29에 어반자카파·펀치·치즈가 서는 공연장에서 **관객 접근성과 관리자 질문 흐름을 미리 검증**하려고 함. 그 자리 관객에게 직장인 닉네임은 안 맞아서 공연 관람객 닉네임이 필요했음.
  - **연동 방식(사용자 선택)**: 관리자·LED는 **그대로 공유**. 입장 주소만 분리했고 채팅·투표·결과는 기존 `/`와 같은 하나의 흐름에 들어간다. 방(room) 분리는 하지 않음.
  - **구현**: chat.html 하나로 유지하고 `THEMES` 객체(닉네임 풀 + 입장 문구)를 두어 주소로 고른다. `/` → office, `/concert` → concert. `?theme=concert` 로도 지정 가능(별도 QR 만들 때 유용). server.js에 `/concert` 라우트 추가(같은 chat.html을 서빙).
  - **닉네임**: concert 테마도 25×25=625개, 최장 13자. 특정 아티스트 이름이나 곡 제목은 넣지 않음(다른 행사에도 재사용 가능하도록).
  - **(2026-08-24 수정) 무료 공연 기준으로 재정리**: 우리가 진행하는 행사는 무료 공연이 많다는 사용자 피드백. 유료 공연·아이돌 팬덤을 전제한 단어를 전부 교체했다. 접두 `티켓팅성공한→소문듣고온`, `응원봉든→광대승천한`, `스탠딩버티는→앞사람머리피하는`, `굿즈지른→친구따라온`, `연차쓰고온→퇴근하고달려온`, `플카든→줄서서기다린`. (`휴대폰불켠`·`서서보는`으로 먼저 바꿨다가 "재미가 없다"는 피드백으로 재교체 — 이 풀의 재미 포인트는 `인트로부터운`·`한곡도못참는`처럼 **행동을 과장한 표현**이지 상황 설명이 아니다. 새 문구를 넣을 땐 이 결을 맞출 것.) 접미 `스탠딩러→뒷줄관객`, `첫공러→첫관람객`, `막공러→감상러`, `올콘러→입장객`, `굿즈러→박수러`, `플카러→응원러`. **앞으로 닉네임을 추가할 때 티켓팅·굿즈·응원봉·플카·스탠딩·첫공/막공/올콘 같은 유료 공연·투어 전제 표현은 쓰지 말 것.**
  - **⚠️ 반드시 지킬 것**: 닉네임 풀은 **chat.html의 `THEMES`와 server.js의 `NICKNAME_THEMES`에 같은 단어가 들어 있어야 한다.** 클라이언트가 로컬에서 즉시 닉네임을 고르지만 동시 접속 중복이 나면 서버가 교체해주는데, 서버 풀이 다르면 공연 화면에 직장인 닉네임이 튀어나온다. `claimNickname`이 `{ name, theme }`을 보내고 서버가 해당 테마 풀에서만 교체하도록 처리했다(문자열도 받도록 하위 호환 유지). 테마를 추가·수정할 땐 양쪽을 함께 고칠 것.
  - **검증(종단)**: `/concert`에서 실제 UI로 입장→채팅 전송 → 기존 `/` 화면과 LED 양쪽에 공연 닉네임 그대로 표시됨 확인. 관리자가 투표 시작 → `/concert`에서 버튼 클릭 → LED 캔버스에 공 반영 확인. `/`의 제목·문구·닉네임은 변경 전과 동일함(회귀 없음).
  - **현장 운영 주의**: Render 무료 플랜은 유휴 시 서버가 잠들어 **첫 접속에 30~60초** 걸린다. 29일 현장에서 관객에게 QR을 열기 **몇 분 전에 미리 주소를 한 번 열어 서버를 깨워둘 것.** (안 그러면 첫 관객이 먹통으로 오해함)
  - 참고: 채팅 기록·투표는 서버 메모리에만 있어 재시작하면 지워진다.
- 2026-08-24: **관리자 화면에 "채팅 초기화" 버튼 추가** (리허설 뒤 본 행사 전에 화면을 비우려고).
  - 서버 `admin:clearChat` → `recentMessages` 비우고 `clearPinnedChat()` 호출 후 `chatCleared` 브로드캐스트. **핀도 함께 내린다** — 지워진 메시지가 LED 중앙에 핀으로 남아 있으면 안 되기 때문.
  - **`recentMessages`를 비우는 게 핵심**: 이걸 안 비우면 화면만 지워지고 나중에 접속하는 관객에게 `history`로 옛 메시지가 다시 내려간다.
  - led.html은 DOM(`container`)과 **일시정지 버퍼(`messages`)를 함께** 비운다. 버퍼를 안 비우면 정지 해제 시 지운 메시지가 되살아난다.
  - chat.html(관객)·admin.html(피드)도 `chatCleared`에서 목록을 비운다. 관리자 버튼은 `confirm()`으로 한 번 확인받는다(되돌릴 수 없음).
  - admin에 `.chat-actions` flex 래퍼 추가 — 멈추기/초기화 버튼을 나란히 두고 좁은 화면에선 줄바꿈.
  - 검증: 초기화 후 신규 접속자 history 0개·핀 없음, 열려 있던 LED/관리자 화면이 실시간으로 비워짐, confirm 취소 시 아무 변화 없음까지 확인.
- 2026-08-24: **Render 계정 이전** — 새 계정에 서비스를 새로 만들어 이전. 새 주소 `https://chat-app-s6y2.onrender.com` (기존 `chat-app-6kl5`도 당분간 살아 있으며 같은 저장소를 보므로 푸시하면 **양쪽에 다 배포된다**. 현장에선 관리자·LED·관객이 모두 같은 주소를 쓰는지 확인할 것).
  - **Render 설정값 (이 저장소는 기본값으로는 배포 실패함)**: Root Directory `chat-app` (저장소 루트에 package.json이 없음 — 안 넣으면 `npm error ENOENT ... /opt/render/project/src/package.json`), Start Command `node server.js` (package.json에 start 스크립트가 없어 `npm start`는 실패). 환경변수는 없음(PORT는 Render가 자동 주입).
  - 저장소가 public이라 GitHub Collaborator 초대 없이 "Public Git Repository"로 바로 연결 가능.
- 2026-08-24: **관리자 화면에 접속자 통계 표시** (현재 접속자 / 최고 동시 접속). "현재 상태" 카드 맨 위에 2줄 추가, 현재 접속자는 초록색으로 강조. (당초 "누적 입장"도 넣었으나 사용자 요청으로 제거 — audienceSeen Set 삭제.) 최고 동시 접속 옆 [초기화] 버튼으로 `admin:resetAudienceStats` → peak를 현재 접속자 수로 되돌림(현재 접속자는 실제 소켓 수라 임의로 못 지운다). 리허설 뒤 본 행사 전 기록만 리셋하는 용도.
  - **관객만 세는 방법**: LED도 `identify`를 호출하므로(led-screen-...) identify로는 구분 못 한다. 관객만 호출하는 `claimNickname`을 관객 마킹 기준으로 삼는다(관리자는 둘 다 안 함). 서버에서 claimNickname 첫 호출 시 `socket.isAudience=true` + clientId를 `audienceSockets`(clientId→소켓수)·`audienceSeen`(누적 Set)에 등록, disconnect에서 해제.
  - **clientId(localStorage 영구 ID) 기준**이라 한 사람이 탭을 여러 개 열어도 현재 접속자는 1명. peak은 최고값 유지, total은 재방문해도 안 늘어남(순 방문자).
  - 접속/해제가 몰릴 때 폭주 방지로 `audienceStats` 브로드캐스트를 250ms 디바운스. publicState에도 `audience` 포함해 관리자가 화면 열 때 즉시 표시.
  - **주의**: 이 통계는 서버 메모리라 재배포·재시작하면 0으로 리셋된다(peak·total 포함). 별도 리셋 버튼은 없음 — 리허설과 본 행사 사이 누적값을 굳이 지울 필요 없다고 보고 안 넣음. 필요하면 추가.
  - 검증: LED 2개 접속해도 현재 0, 관객 2명→2, 같은 clientId 탭 추가→2 유지, 1명 퇴장→1(peak 2 유지), 전원 퇴장→0, 재입장→누적 그대로. 실제 관리자 UI 렌더까지 확인.
- 2026-08-29: **콘서트 현장용 가로형 LED 페이지 추가 — `/led/concert`** (led-concert.html).
  - **이유**: 세로형(2:3) led.html을 현장 가로 LED(2496×1872 = **4:3**)에 띄우니 좌우 여백이 크게 남아 콘텐츠가 가운데 좁은 세로 띠에만 나오고 **글자가 작아 안 보이는** 문제. 관리자·관객은 기존 것 그대로 공유하고 LED만 별도 페이지로 분리(사용자 요청).
  - **led.html 복사 후 가로형 조정**: `#screen` 종횡비 4:3(`width: min(100vw, calc(100vh*4/3))`), 논리 기준폭 `BASE_W` 800→1040. 라우트 `/led/concert` 추가(server.js). **관리자/관객/서버 로직은 전혀 안 건드림** — 같은 채팅·투표에 그대로 참여.
  - **가독성 확대**(원래 문제 해결): 질문 22→34px, 타이머 배지 56→70px, 채팅 본문 17→22px·닉네임 11→14px, 결과 % 17→24px, 상자 헤더 라벨 21→27px. (모두 calc(Npx*var(--u)) 형태 유지 — 해상도 독립.)
  - **채팅 말풍선**: 세로형은 `align-items:stretch`+`max-width:100%`로 "라인 맞춤"이지만, 가로형에선 짧은 글도 화면 폭 전체로 늘어나 부자연스러움 → `.led-msg-group`에 `align-items:flex-start` + `max-width:62%`로 iMessage처럼 내용 크기에 맞춤.
  - **핵심: 보기 배치를 한 줄로**(`clusterLayout`이 항상 `{cols:n, rows:1}`). 세로형은 4개↑를 2열로 나눴는데, 가로형에서 그러면 상자가 납작해져 공이 옆으로만 깔리고 표 차이가 안 보였음. 한 줄로 두면 상자가 세로로 길어져 공이 쌓이고 비율이 한눈에 읽힘. 6지선다까지 확인.
  - **검증(스크린샷, 2496×1872)**: 채팅(iMessage 정렬)·2/3/4/6지선다·0표·결과 %배너·이모지·워드클라우드·채팅핀 전 상태 확인. `--u` 덕에 절반 해상도(1248×936)로 찍어도 비율 동일.
  - **현장 3주소**: 관객 `/concert`, LED `/led/concert`, 관리자 `/admin`.
